@model IMS.Application.Models.BranchesPageModel
@{
    ViewBag.Title = "List of Dealers";
}

<script src="../../Scripts/Request/Agency.js" type="text/javascript"></script>
<link href="../../Content/Admin.css" rel="stylesheet" type="text/css" />
<script type="text/javascript" src="../../Scripts/Helper/humane.min.js"></script>

<div class = "main-input-wrap">
<div style=" padding: 30px 30px 0px 30px">
    <form id="input-form" name="input-form" style=" padding: 20px">
    @Html.ValidationSummary(true, "Agent creation was unsuccessful. Please correct the errors and try again.")
    <div>
        <fieldset>
            <legend>Branches Information</legend>
            <div class="editor-label">
                @Html.LabelFor(m => m.CurrentBranch.Branch)
            </div>
            <div class="editor-field">
                @Html.TextBoxFor(m => m.CurrentBranch.Branch)
                @Html.ValidationMessageFor(m => m.CurrentBranch.Branch)
            </div>
            <div class="editor-label">
                @Html.LabelFor(m => m.CurrentBranch.Address)
            </div>
            <div class="editor-field">
                @Html.TextBoxFor(m => m.CurrentBranch.Address)
                @Html.ValidationMessageFor(m => m.CurrentBranch.Address)
            </div>
            <div class="editor-label">
                @Html.LabelFor(m => m.CurrentBranch.IsActive)
            </div>
            <div class="editor-field">
                @Html.TextBoxFor(m => m.CurrentBranch.IsActive, new { @type = "hidden" })
                @Html.ValidationMessageFor(m => m.CurrentBranch.IsActive)
                <SELECT name="isValidOption" id="isValidOption">
                 <OPTION VALUE="True">True
                 <OPTION VALUE="False" SELECTED>False
                </SELECT>
            </div>
            <div class="editor-field" hidden="hidden">
                @Html.TextBoxFor(m => m.PkeyForView)
            </div>
            <hr />
            <p>
                <input type="submit" value="Add" id="cmdAdd"/>
                <input type="submit" value="Remove" id="cmdRemove" />
                <input type="submit" value="Update" id="cmdUpdate" />
            </p>
        </fieldset>
    </div>
    </form>
    </div>
<hr />

<h2>List of Branches</h2>
<div class="main-grid-wrap" style=" padding: 20px" >
<table id="agent-list">
    <tr>
        <th>
            Branches Name
        </th>
        <th>
            Address
        </th>
        <th>
            Active
        </th>
        <th style="display:none"></th>
        <th></th>
    </tr>

    @foreach (var item in Model.AllBranches) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Branch)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Address)
        </td>
        <td>
            @Html.CheckBoxFor(modelItem => item.IsActive)
        </td>
        <td style="display:none">
            @Html.DisplayFor(modelItem => item.PK_MstrBranches)
        </td>
    </tr>
}
</table>
</div>
</div>