@model IMS.Application.Models.ItemPageModel
@{
    ViewBag.Title = "List of Items";
}

<script src="../../Scripts/Request/Agency.js" type="text/javascript"></script>
<link href="../../Content/Admin.css" rel="stylesheet" type="text/css" />
<script type="text/javascript" src="../../Scripts/Helper/humane.min.js"></script>

<div class = "main-input-wrap">
<div style=" padding: 30px 30px 0px 30px">
    <form id="input-form" name="input-form" style=" padding: 20px">
    @Html.ValidationSummary(true, "Agent creation was unsuccessful. Please correct the errors and try again.")
    <div>
        <fieldset>
            <legend>Item Information</legend>
            <div class="editor-label">
                @Html.LabelFor(m => m.CurrentItem.ItemCode)
            </div>
            <div class="editor-field">
                @Html.TextBoxFor(m => m.CurrentItem.ItemCode)
                @Html.ValidationMessageFor(m => m.CurrentItem.ItemCode)
            </div>
            <div class="editor-label">
                @Html.LabelFor(m => m.CurrentItem.ItemDescription)
            </div>
            <div class="editor-field">
                @Html.TextBoxFor(m => m.CurrentItem.ItemDescription)
                @Html.ValidationMessageFor(m => m.CurrentItem.ItemDescription)
            </div>
            <div class="editor-label">
                @Html.LabelFor(m => m.CurrentItem.Model)
            </div>
            <div class="editor-field">
                @Html.TextBoxFor(m => m.CurrentItem.Model)
                @Html.ValidationMessageFor(m => m.CurrentItem.Model)
            </div>
            <div class="editor-label">
                @Html.LabelFor(m => m.CurrentItem.IsActive)
            </div>
            <div class="editor-field">
                @Html.TextBoxFor(m => m.CurrentItem.IsActive, new { @type = "hidden" })
                @Html.ValidationMessageFor(m => m.CurrentItem.IsActive)
                <SELECT name="isValidOption" id="isValidOption">
                 <OPTION VALUE="True">True
                 <OPTION VALUE="False" SELECTED>False
                </SELECT>
            </div>
            <div class="editor-field" hidden="hidden">
                @Html.TextBoxFor(m => m.PkeyForView)
            </div>
            <hr />
            <p>
                <input type="submit" value="Add" id="cmdAdd"/>
                <input type="submit" value="Remove" id="cmdRemove" />
                <input type="submit" value="Update" id="cmdUpdate" />
            </p>
        </fieldset>
    </div>
    </form>
    </div>
<hr />

<h2>List of Users</h2>
<div class="main-grid-wrap" style=" padding: 20px" >
<table id="agent-list">
    <tr>
        <th>
            User Code
        </th>
        <th>
            User Name
        </th>
        <th>
            Password
        </th>
        <th>
            Confirm Password
        </th>
        <th>
            Is Active
        </th>
        <th>
            Is Admin
        </th>
        <th style="display:none"></th>
        <th></th>
    </tr>

    @foreach (var item in Model.AllItems) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ItemCode)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ItemDescription)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Model)
        </td>
        <td>
            @Html.CheckBoxFor(modelItem => item.IsActive)
        </td>
        <td style="display:none">
            @Html.DisplayFor(modelItem => item.PK_MstrItems)
        </td>
    </tr>
}
</table>
</div>
</div>